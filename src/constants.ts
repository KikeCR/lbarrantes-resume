// Shared `react-awesome-reveal` <Fade> duration, in ms, for the slower/more
// deliberate reveals (Navbar title, Experience timeline) — kept as a named
// constant since it's reused across files and easy to mistake for an
// arbitrary magic number otherwise.
export const FADE_DURATION_SLOW = 2000
